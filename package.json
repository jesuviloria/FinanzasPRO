{
  "name": "package.json",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "json-server": "npx json-server --watch public/database/db.json"
  },
  "devDependencies": {
    "vite": "^7.0.4"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
